import { Check } from "lucide-react";

const CheckCircleIcon = () => (
    <div className="bg-teal-600 text-white rounded-full p-1 shadow-sm">
        <Check size={14} strokeWidth={3} />
    </div>
);

export default CheckCircleIcon;